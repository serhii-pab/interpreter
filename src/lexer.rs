

#[cfg(test)]
mod test {
    use crate::token::Token;

    #[test]
    fn test_next_token() {
        let input = "=+(){},;".to_string();

        let lexer = Lexer::new(input);

        let tests
    }
}
